Ignore the main.ipynb for now.
Use data to store processed data.
Models folder to store the models we made so we can use it.
Notebooks are the ones where we will store our other files like "eda" "modelling" "pipelines" (depending on whatever we are creating).
